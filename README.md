```bash
cd <projectdir>
npm run unroll # npm i's in api and ui dirs
cd api
npm start # spools up backend @ localhost:3001 or next available
<new terminal window>
cd <projectdir>/ui
npm start # spools up frontend @ localhost:3000 or next available
```